<header class='site-header'>

  <!-- Main container -->
  <nav class="navbar">
    <!-- Left side -->
    <div class="navbar-left">

      <div class="navbar-item">
        <h1 class="site-title">Syncline</h1>
      </div>
      <div class="navbar-item">
        <%= link_to "All Sites", sites_path, :class => "button is-primary" %>
        <a class="button is-primary">Add Site</a>
      </div>

    </div>

    <!-- Right side -->
    <div class="navbar-right">
      <% if current_user %>
        <p class="navbar-item"><a href="#">Hey, <%= current_user.first_name %></a></p>
        <p class="navbar-item"><%= link_to "Log out", session_path("current"), method: :delete %></p>
      <% else %>
        <p class="navbar-item"><%= link_to "Log In", new_session_path %> | <%= link_to "Sign Up", new_user_path %></p>
      <% end %>
    </div>
  </nav>

</header>

<!-- <footer>
  <small>
    <% if current_user %>
      Signed in as <%= current_user.full_name %> (<%= link_to "Log out", session_path("current"), method: :delete %>)
      <% if current_user.admin %>
        See all <%= link_to "Users", admin_users_path %>
        <% end %>
    <% else %>
      <%= link_to "Log In", new_session_path %> | <%= link_to "Sign Up", new_user_path %>
    <% end %>
  </small>
</footer> -->
